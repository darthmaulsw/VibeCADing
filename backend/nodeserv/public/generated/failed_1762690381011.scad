
// Single solid sphere parameters
radius_mm = 20; // sphere radius in millimeters
fn = 96;        // polygonal resolution for smoothness

// Generate sphere centered at origin
sphere(r=radius_mm, $fn=fn);
